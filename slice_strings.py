sample_url = 'http://nokea360.com'
print(sample_url)

print(sample_url[::-1])       # Reversing the url
print(sample_url[-4:])        # Get the top level domain
print(sample_url[7:])         # Prints the url without the http://
print(sample_url[7:-4])       # Prints the url without the http:// or the top level domain
